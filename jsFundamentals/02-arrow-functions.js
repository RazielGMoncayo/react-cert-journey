

//? Arrow fuction concise body 
let printName = name => "Hello " + name 
console.log(printName("Raziel"))

//* Arroy fuction with backticks 
let printNameAge = (name, age) => `Hello ${name}, your age is ${age}`

//! Arroy function block body 
let sum = (a, b) => {
    return a + b
} 
